apiVersion: apps/v1
kind: Deployment
metadata:
  name: dotnetmvc-deployment
  namespace: dotnet
spec:
  selector:
    matchLabels:
      name: podkoname
  replicas: 2
  template:
    metadata:
      labels:
        name: podkoname
    spec:
      containers:
      - name: dotnetmvccontainer
        image: rajanup77/dotnet-mvc
        ports:
        - containerPort: 5206

